CREATE OR REPLACE TRIGGER auditaEmpleados
BEFORE INSERT OR UPDATE 
ON DIR
FOR EACH ROW 
DECLARE 
    jefe EMPLEADOS.EMP_NO_COORD%TYPE;
BEGIN
    SELECT EMP_NO_EMPLEADO INTO jefe 
    FROM DIR 
    WHERE EMP_NO_EMPLEADO = EMP_NO_JEFE;
END;